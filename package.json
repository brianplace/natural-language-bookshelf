{
    "name": "bookshelf",
    "private": true,
    "scripts": {
        "dev": "pnpm --parallel -r dev",
        "inspect": "npx concurrently \"pnpm --filter api dev\" \"pnpm --filter mcp dev\" \"npx @modelcontextprotocol/inspector\""
    },
    "workspaces": [
        "packages/*"
    ],
    "devDependencies": {
        "next": "16.1.6"
    }
}
